export class SleepDay {
    id: number;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    current_sleepiness: string;
    reasons: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    amount_bothered: string;
}
